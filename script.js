var typed = new Typed(".typewriter-text", {
    strings: [
        "Developer",
        "Designer",
        "Freelancer",
        "Blogger"
    ],
    typeSpeed: 75,        // Typing speed reduced for smoother effect
    backSpeed: 50,        // Backspacing speed
    startDelay: 500,      // Delay before typing starts (in milliseconds)
    backDelay: 1500,      // Delay before backspacing starts (in milliseconds)
    loop: true,           // Keep looping through the words
    showCursor: true,     // Show the blinking cursor
    cursorChar: '|',      // Customize cursor character
    autoInsertCss: true   // Automatically inject CSS for the typing effect
});
